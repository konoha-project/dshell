function func() {
	var a = 123
	var b = 2.0
	var c = "4567"

	# int -> float
	var d = (float)a

	# String -> int
	var e = (int)c

	# float -> String
	var f = (String)b

	assert(d instanceof float)
	assert(e instanceof int)
	assert(f instanceof String)

}

func()
